# A3 - Manhattan

Find the best path through a grid, maximizing the sum of edge weights.


## Usage

### Synopsis

```bash
juwei95-Manhattan.py [-h] [-t] [-d] [-b] filename
```

### Positional arguments

| Argument   | Description                                 |
| ---------- | ------------------------------------------- |
| `filename` | The input file containing the edge matrices |

### Options

| Opt  | Option       | Description                    |
| ---- | ------------ | ------------------------------ |
| `-h` | `--help`     | Show a help message and exit   |
| `-t` | `--trace`    | Print the best path            |
| `-d` | `--diagonal` | Allow diagonal edge weights    |
| `-b` | `--bellman`  | Use Bellman-Ford instead of DP |

### Example

```bash
python3 juwei95-Manhattan.py Manhattan-testHV1.in
```

### Tests

* Use [`./test-A3.sh`](./test-A3.sh) to run the automated tests.
* **Disclaimer:** Test script was generated by AI.


## Input

A file containing edge weight matrices.
Down and right edges are mandatory, diagonal edges are only required if `-d` is specified.

### Example

```
#G_down: 4 5
  0.60   0.65   0.91   0.94   0.14
  0.85   0.27   0.70   0.31   0.63
  0.63   0.23   0.35   0.77   0.20
  0.37   0.76   0.41   0.30   0.67
#---
#G_right: 5 4
  0.76   0.41   0.72   0.13
  0.57   0.64   0.62   0.62
  0.37   0.98   0.36   0.24
  0.99   0.77   0.39   0.35
  0.37   0.34   0.62   0.82
#---
#G_diag: 4 4
  6.74   7.03   2.47   6.25
  4.48   3.75   2.98   3.62
  7.90   3.63   3.67   3.18
  9.30   8.40   9.02   2.58
#---
```


## Output

* Prints the maximal sum of edge weights to `stdout`.
* If `-t` is given, additionally prints the chosen path through the grid.

### Example

```
58.20
EDDEDSSDDDD
```


## Details

* By default, computes a DP matrix over a regular grid.
* If `-b` is given, instead performs breadth-first wavefront-propagation to compute the matrix, similar to [Bellman-Ford algorithm](https://en.wikipedia.org/wiki/Bellman%E2%80%93Ford_algorithm).
* Found paths are reconstructed using traceback.
* Implemented in python using only standard library modules.
* Tested using python 3.12.12 on Ubuntu 24.04.3 LTS under WSL.


## Benchmark

Runtimes of the implemented solution against the provided test cases were measured using [test-A3.sh](./test-A3.sh).
The performance of three different implementations was compared:
* **DP (Python)**: The primary assignment solution (`juwei95-Manhattan.py`). Handwritten in Python; uses a DP matrix.
* **Bellman-Ford (Python) [ms]**: Uses [Bellman-Ford algorithm](https://en.wikipedia.org/wiki/Bellman%E2%80%93Ford_algorithm) instead of DP (`juwei95-Manhattan.py -b`). Handwritten in Python.
* **Bellman-Ford (C++)**: C++ implementation of the Bellman-Ford solution (`test-Manhattan.cpp`). AI-generated using the Python version as a specification.

| Test case             | DP (Python) [ms] | Bellman-Ford (Python) [ms] | Bellman-Ford (C++) [ms] |
| --------------------- | ---------------: | -------------------------: | ----------------------: |
| Manhattan-testHV1.in  |               23 |                         27 |                       2 |
| Manhattan-testHV2.in  |               27 |                         26 |                       1 |
| Manhattan-testHV3.in  |               36 |                         35 |                       3 |
| Manhattan-testHVD1.in |               27 |                         26 |                       2 |
| Manhattan-testHVD2.in |             1592 |                       1973 |                     342 |

The C++ implementation was generated using the following prompt:

> Translate the following python program to C++.
> Make sure to satisfy the following requirements:
> - no external dependencies
> - I/O must work on linux, preferably crossplatform
> - aim for performance, but do not change semantics
> - include instructions to build and run the program
